<view class="card-trip">

  <!-- <view wx:for="{{ services }}" wx:for-item="service" class="card-category" data-id="{{ service.id }}"> -->
      <image mode="aspectFill" class="model-photo" src="{{ images[service.id - 1].image }}"/>
  <!-- </view> -->

 <!-- <image class= "image" wx:for="{{ images }}" wx:for-item="image" src="{{ images[index].image }}" /> -->

  <view class="card-trip-infos">
    <view class="h2"><strong>Model:</strong> {{ service.name }}</view>
  </view>
</view>

<view class="card-trip-infos">
  <text>Description:<text>\n\n</text> {{ service.description }}</text>
</view>   
<text>\n</text>
<button class="btn" bindtap="buttonClicked">Book</button>