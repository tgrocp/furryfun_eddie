<view class="card-trip">
 <image class= "image"src="{{ images.image }}" />
  <view class="card-trip-infos">
    <view class="h2"><strong>Model:</strong> {{ pet.pet_name }}</view>
  </view>
</view>

<view class="card-trip-infos">
  <text>Description:<text>\n\n</text> {{ pet.description }}</text>
</view>   
<text>\n</text>
<button class="btn" bindtap="buttonClicked" mark:serviceId = "{{service.id}}">Book</button>
<button class="btn" bindtap="deleteModel" data-id="{{service.id}}">Delete</button>

<tabBar></tabBar>

