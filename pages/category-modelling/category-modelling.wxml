<view class="cards">
<view wx:for="{{services}}" wx:for-item="service" class="card-category" bindtap="onClick" data-id="{{service.id}}">
      <image mode="aspectFill" class="model-photo" src="{{models.image}}"/>
      <view class="h1">{{service.name}}</view>
      <view class="h2">{{service.title}}</view>
      <!-- <text>{{service.description}}</text> -->
  </view>
</view>